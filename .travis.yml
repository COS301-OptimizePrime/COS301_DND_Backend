
language: python, node
python:
  - "3.6"

install:
  - pip install --user -r requirements.txt
  - npm install

branches:
  only:
  - master
  - stable
  - develop

script:
  - make run
  - make test
