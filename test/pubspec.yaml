name: COS301_DND_Backend
version: 0.0.1
description: >-
  Runs the api server
environment:
  sdk: '<=2.1.0'

dependencies:
  async: '>=1.13.3 <3.0.0'
  protobuf: ^0.10.4
  http: ^0.11.3+16
  http2: ^0.1.7
  grpc: ^0.6.1
  firebase: ^5.0.1
  js: ^0.6.0
  protoc_plugin: ^0.10.4

dev_dependencies:
  test: ^1.3.0
